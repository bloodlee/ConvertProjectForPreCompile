The project is used for those C++/VC++ project without pre-compiled header.
The main script will extract the common used header to a pre-compiled header.

The usage is:
-----------------------
ConvertFileForPreCompile.py [vcproject filename or source file list]
-----------------------